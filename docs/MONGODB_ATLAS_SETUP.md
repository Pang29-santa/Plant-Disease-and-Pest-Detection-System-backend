# วิธีสร้าง Database บน MongoDB Atlas

## วิธีที่ 1: ให้ MongoDB สร้างอัตโนมัติ (แนะนำ) ✅

Database `mydb` จะถูกสร้างอัตโนมัติเมื่อคุณเพิ่มข้อมูลครั้งแรกผ่าน API

### ทดสอบโดยการเรียก API:
```bash
# ตัวอย่าง: สร้าง user ใหม่
curl -X POST http://localhost:8888/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "password123",
    "email": "test@example.com"
  }'
```

หลังจากนี้ database `mydb` และ collection `users` จะถูกสร้างบน MongoDB Atlas อัตโนมัติ

---

## วิธีที่ 2: สร้างผ่าน MongoDB Atlas Web UI 🌐

### ขั้นตอนที่ 1: เข้าสู่ MongoDB Atlas
1. ไปที่ https://cloud.mongodb.com/
2. Login ด้วย account ของคุณ

### ขั้นตอนที่ 2: เลือก Cluster
1. คลิกที่ Cluster ของคุณ (ชื่อ: `Project`)
2. คลิกปุ่ม **"Browse Collections"**

### ขั้นตอนที่ 3: สร้าง Database
1. คลิกปุ่ม **"+ Create Database"**
2. กรอกข้อมูล:
   - **Database Name**: `mydb`
   - **Collection Name**: `users` (หรือ collection อื่นที่ต้องการ)
3. คลิก **"Create"**

### ขั้นตอนที่ 4: เพิ่ม Collections อื่นๆ (ถ้าต้องการ)
คลิกที่ database `mydb` แล้วคลิก **"+ Create Collection"** เพื่อสร้าง:
- `vegetables` - ข้อมูลผัก
- `diseases_pests` - ข้อมูลโรคและศัตรูพืช
- `planting` - ข้อมูลการปลูก
- `harvesting` - ข้อมูลการเก็บเกี่ยว
- `locations` - ข้อมูลสถานที่
- `user_vegetables` - ผักของผู้ใช้
- `otp_codes` - รหัส OTP

---

## วิธีที่ 3: สร้างผ่าน Python Script 🐍

ให้ผมสร้างสคริปต์สำหรับสร้าง database และ collections ให้:

```python
# ดูไฟล์: create_database.py
```

---

## ตรวจสอบว่า Database ถูกสร้างแล้ว

### วิธีที่ 1: ผ่าน API
```bash
curl http://localhost:8888/health
```

ผลลัพธ์:
```json
{
  "status": "OK",
  "timestamp": "2026-02-13T...",
  "database": "Connected"
}
```

### วิธีที่ 2: ผ่าน MongoDB Atlas UI
1. ไปที่ https://cloud.mongodb.com/
2. คลิก **"Browse Collections"**
3. จะเห็น database `mydb` และ collections ต่างๆ

---

## Collections ที่ควรมีใน Database `mydb`

ตาม API Routes ของคุณ ควรมี collections เหล่านี้:

| Collection Name | คำอธิบาย |
|----------------|---------|
| `users` | ข้อมูลผู้ใช้ |
| `vegetables` | ข้อมูลผัก |
| `diseases_pests` | โรคและศัตรูพืช |
| `planting` | ข้อมูลการปลูก |
| `harvesting` | ข้อมูลการเก็บเกี่ยว |
| `locations` | สถานที่ปลูก |
| `user_vegetables` | ผักของผู้ใช้แต่ละคน |
| `otp_codes` | รหัส OTP สำหรับยืนยันตัวตน |

---

## หมายเหตุ

- MongoDB Atlas จะสร้าง database และ collection อัตโนมัติเมื่อมีการเพิ่มข้อมูลครั้งแรก
- ไม่จำเป็นต้องสร้างล่วงหน้า เว้นแต่ต้องการกำหนด indexes หรือ validation rules
- Database `mydb` จะอยู่บน Cloud (MongoDB Atlas) ไม่ใช่ในเครื่อง
