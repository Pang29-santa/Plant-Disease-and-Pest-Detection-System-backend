# API ระบบจัดการแปลงผักและเกษตร (Vegetable & Agriculture Backend)

ระบบหลังบ้าน (Backend) สำหรับจัดการข้อมูลการปลูกผัก พื้นที่เกษตร และวิเคราะห์โรคพืช พัฒนาด้วย **FastAPI** และ **MongoDB**

## 🚀 ฟีเจอร์หลัก (Features)

- **ระบบสมาชิก (Users & Auth)**: เข้าสู่ระบบด้วย JWT, แยกสิทธิ์ Admin/User
- **ฐานข้อมูลผัก (Vegetable DB)**: จัดการข้อมูลสายพันธุ์ผัก ระยะเวลาเก็บเกี่ยว และคำแนะนำการปลูก
- **AI ตรวจโรคพืช (AI Disease Detection)**: วิเคราะห์โรคและแมลงศัตรูพืชจากรูปภาพ
- **จัดการแปลงปลูก (Plot Management)**: บันทึกข้อมูลแปลงปลูก ติดตามการเจริญเติบโต และกำหนดการเก็บเกี่ยว
- **ระบบติดต่อ (Contact & Support)**: ส่งข้อความหาแอดมินผ่านอีเมล (เชื่อมต่อ Gmail API)
- **การแจ้งเตือน (Notifications)**: แจ้งเตือนผ่าน Telegram Bot

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **Framework**: FastAPI (Python 3.10+) - ทำงานเร็ว รองรับ Asynchronous
- **Database**: MongoDB (ผ่าน Motor async driver) - เก็บข้อมูลแบบ NoSQL
- **Authentication**: JWT (JSON Web Tokens) - ปลอดภัยและเป็นมาตรฐาน
- **External Services**:
  - **Gmail API**: สำหรับส่งอีเมลแจ้งเตือน/ติดต่อ
  - **Telegram Bot API**: สำหรับแจ้งเตือนไปยังมือถือ
  - **OpenAI / Kimi AI**: สำหรับช่วยวิเคราะห์ข้อมูลเชิงลึก

## 📦 การติดตั้ง (Installation)

1. **Clone โปรเจกต์**
   ```bash
   git clone <repository_url>
   cd backend_fastapi
   ```

2. **สร้าง Virtual Environment** (แนะนำ)
   ```bash
   # สำหรับ Windows
   python -m venv .venv
   .\.venv\Scripts\activate
   
   # สำหรับ Mac/Linux
   # python3 -m venv .venv
   # source .venv/bin/activate
   ```

3. **ติดตั้ง Library ที่จำเป็น**
   ```bash
   pip install -r requirements.txt
   ```

4. **ตั้งค่า Environment Variables**
   สร้างไฟล์ `.env` (ก๊อปปี้จาก `.env.example` ถ้ามี) และใส่ค่าต่างๆ:
   ```env
   MONGODB_URI=mongodb+srv://... (Connection string ของ MongoDB)
   SECRET_KEY=... (รหัสลับสำหรับ JWT)
   
   # ตั้งค่า Gmail API
   GMAIL_CLIENT_ID=...
   GMAIL_CLIENT_SECRET=...
   GMAIL_REFRESH_TOKEN=...
   ```

## 🚦 การรันเซิร์ฟเวอร์ (Running)

รันด้วยคำสั่ง Python ปกติ:
```bash
python main.py
```

หรือรันด้วย Uvicorn โดยตรง (สำหรับการพัฒนา):
```bash
uvicorn main:app --reload --port 8888
```

- **API URL**: `http://localhost:8888`
- **เอกสาร API (Swagger UI)**: `http://localhost:8888/docs`

## 📧 การตั้งค่าอีเมล (Gmail API Setup)

โปรเจกต์นี้ใช้ **Gmail API (OAuth2)** แทนการใช้ SMTP แบบเก่า เพื่อความเสถียรและความปลอดภัยที่ดีกว่า

1. สร้าง Project ใน [Google Cloud Console](https://console.cloud.google.com/)
2. เปิดใช้งาน **Gmail API**
3. สร้าง OAuth 2.0 Credentials (จะได้ Client ID และ Client Secret)
4. เพิ่ม `http://localhost:8888` ในช่อง Authorized Redirect URIs
5. นำค่า Key มาใส่ใน `.env` ให้ครบถ้วน

## 📁 โครงสร้างโปรเจกต์ (Project Structure)

```
backend_fastapi/
├── main.py              # จุดเริ่มต้นของโปรแกรม (Entry point)
├── database.py          # การเชื่อมต่อ MongoDB
├── routes/              # ตัวจัดการ API URL ต่างๆ (Controllers)
│   ├── contact.py       # API ติดต่อสอบถาม
│   ├── users.py         # API สมาชิก
│   └── ...
├── services/            # ส่วนทำงานเบื้องหลัง (Business Logic)
│   ├── email_service.py # จัดการส่งอีเมล
│   └── ...
├── models/              # โครงสร้างข้อมูล (Schemas/Pydantic)
├── utils/               # ฟังก์ชันช่วยเหลือทั่วไป
└── static/              # ไฟล์รูปภาพ/ไฟล์อัปโหลด
```

## 🤝 การช่วยพัฒนา (Contributing)

ยินดีต้อนรับการแก้ไขและพัฒนาต่อ!
1. Fork repository นี้
2. สร้าง branch ใหม่ (`git checkout -b feature/MyFeature`)
3. Commit การแก้ไข (`git commit -m 'Add MyFeature'`)
4. Push ไปยัง branch (`git push origin feature/MyFeature`)
5. เปิด Pull Request มาที่ main branch
